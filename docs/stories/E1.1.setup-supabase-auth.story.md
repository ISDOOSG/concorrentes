# Story E1.1 — Setup Supabase Auth + migration inicial

**Status:** Draft
**Epic:** [E1 — Auth + Onboarding](../epics/E1-auth-onboarding.md)
**Created by:** @sm (River)
**Created at:** 2026-04-26

## User Story

**Como** desenvolvedor do projeto
**Quero** habilitar Supabase Auth e ter a tabela `profiles` provisionada com RLS desde a primeira migration
**Para** que toda story subsequente possa assumir um modelo single-tenant correto e testável.

## Acceptance Criteria

- [ ] Migration `supabase/migrations/0001_init.sql` aplicada com sucesso (`supabase db push`)
- [ ] Tabela `public.profiles` criada com colunas: `id (uuid, FK auth.users)`, `full_name text`, `plan text default 'free'`, `url_quota int default 5`, `created_at timestamptz`
- [ ] RLS habilitado em `profiles` com 4 policies (select/insert/update/delete) usando `auth.uid() = id`
- [ ] Trigger `on_auth_user_created` invoca função `handle_new_user()` que insere row em `profiles` automaticamente após signup
- [ ] Provider e-mail/senha habilitado no Supabase Dashboard (manual, mas documentado no story)
- [ ] Tipos TypeScript regenerados via `bun run db:types` (script novo no `package.json`) e commitados em `src/integrations/supabase/types.ts`
- [ ] `bun install` roda sem erros e `bun run dev` sobe o app sem regressão

## Tasks / Subtasks

- [ ] **T1** Criar migration `supabase/migrations/0001_init.sql`
  - [ ] T1.1 DDL `profiles` conforme architecture.md §3.1
  - [ ] T1.2 RLS + 4 policies (`auth.uid() = id`)
  - [ ] T1.3 Função `handle_new_user()` SECURITY DEFINER
  - [ ] T1.4 Trigger `on_auth_user_created after insert on auth.users`
- [ ] **T2** Verificar Supabase CLI linkado ao projeto
  - [ ] T2.1 Confirmar `supabase/config.toml` aponta para `xkdvfopvassssvkuxjnp`
  - [ ] T2.2 Rodar `supabase login` e `supabase link` se necessário
- [ ] **T3** Aplicar migration
  - [ ] T3.1 `supabase db push`
  - [ ] T3.2 Validar no dashboard que `profiles` aparece e RLS está ativo
- [ ] **T4** Habilitar provider e-mail/senha no Dashboard Supabase (manual)
  - [ ] T4.1 Confirmar disable de "Confirm email" em DEV (acelerar testes); manter ON em PROD
- [ ] **T5** Adicionar script `db:types` em `package.json`
  - [ ] T5.1 `"db:types": "supabase gen types typescript --project-id xkdvfopvassssvkuxjnp > src/integrations/supabase/types.ts"`
  - [ ] T5.2 Rodar e commitar `types.ts`
- [ ] **T6** Smoke test: `bun install` + `bun run dev` rodando sem erros

## Dev Notes

- **Caminho do cliente Supabase:** `src/integrations/supabase/client.ts` já existe, conferir que importa `types.ts` correto
- **Função `handle_new_user`:** insere apenas `id`; `full_name`, `plan`, `url_quota` ficam nos defaults
- **`SECURITY DEFINER`:** a função precisa rodar com elevação para conseguir inserir em `profiles` durante o trigger (RLS bloquearia o usuário recém-criado)
- **Confirm email em DEV:** desabilitar acelera dev mas precisa ser re-habilitado antes de prod. Adicionar TODO.

### Estrutura da migration esperada (referência, não cole literal — adapte)

```sql
-- 0001_init.sql
create extension if not exists "pgcrypto";

create table public.profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  full_name text,
  plan text not null default 'free' check (plan in ('free', 'pro')),
  url_quota int not null default 5,
  created_at timestamptz not null default now()
);

alter table public.profiles enable row level security;

create policy "users_select_own_profile" on public.profiles
  for select using (auth.uid() = id);
create policy "users_insert_own_profile" on public.profiles
  for insert with check (auth.uid() = id);
create policy "users_update_own_profile" on public.profiles
  for update using (auth.uid() = id);
create policy "users_delete_own_profile" on public.profiles
  for delete using (auth.uid() = id);

create or replace function public.handle_new_user()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  insert into public.profiles (id) values (new.id);
  return new;
end;
$$;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();
```

## Testing

- [ ] Manual: criar usuário via Supabase Dashboard → confirmar row em `profiles`
- [ ] Manual: tentar `select * from profiles` sem JWT → deve retornar 0 rows (RLS bloqueando)
- [ ] Smoke: app sobe e nenhum log de erro no console relativo a Supabase

## File List (preenchido pelo @dev)

- `supabase/migrations/0001_init.sql` (criado)
- `package.json` (script `db:types` adicionado)
- `src/integrations/supabase/types.ts` (regenerado)

## Change Log

| Data | Versão | Mudança | Autor |
|------|--------|---------|-------|
| 2026-04-26 | 0.1 | Draft inicial | @sm |
